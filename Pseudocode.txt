####################################################################################
#Here the pseudocode for the project is stored, including functions and what they do
####################################################################################

#Instruments
- Barometer (pressure, altitude, temperature(?))
- Temperature (outside)
- 

#Function
##Get voltage function
float getVoltage(int pin) {
  int raw = analogRead(pin);
  float voltage = raw * 5.0/1023.0;
  return voltage;
}
